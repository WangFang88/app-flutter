# 📋 项目上架风险全面分析报告

## 项目概览

本项目是一个"协同提醒"应用（`reminder_app`），核心功能是用户创建提醒，其他用户可以"提醒他"增加通知强度。通知系统分为三级强度（低/中/高），每5分钟重复一次，持续1小时。

---

## 🔴 严重问题（会导致上架被拒）

### 问题 1：iOS `NSAllowsArbitraryLoads = true` — 必定被拒

**位置：** `ios/Runner/Info.plist` 第31行

**现状：** 设置了 `NSAllowsArbitraryLoads = true`，完全禁用了 App Transport Security (ATS)，允许所有 HTTP 明文连接。还硬编码了一个局域网 IP `192.168.124.14` 作为例外域名。

**为什么会被拒：**
Apple 自 iOS 9 起要求所有网络连接使用 HTTPS。`NSAllowsArbitraryLoads = true` 只在极少数场景下被允许（如浏览器类应用需要加载第三方网页）。本应用是提醒工具，不构成例外理由。

> **Apple 审核指南 2.5.1：** "Apps must use HTTPS for all network communications. Apps that use ATS exceptions must provide a clear justification in the review notes."

**如何修改：**
1. 在 `Info.plist` 中将 `NSAllowsArbitraryLoads` 改为 `false`
2. 删除 `192.168.124.14` 例外域名（这是开发配置）
3. 如果确实需要连接特定 HTTP 域名，使用 `NSExceptionDomains` 针对性配置
4. 将后端 API 迁移到 HTTPS（这是根本解决方案）

```xml
<!-- 修改后的 NSAppTransportSecurity -->
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <false/>
    <!-- 如果确实需要访问特定 HTTP 域名，用 NSExceptionDomains 针对性声明 -->
</dict>
```

---

### 问题 2：默认 API 地址使用 HTTP 明文 — 双平台受影响

**位置：** `lib/data/api_service.dart` 第9行

**现状：**
```dart
static const baseUrl = String.fromEnvironment(
  'API_BASE_URL',
  defaultValue: 'http://10.0.2.2:8080',
);
```

默认值是 `http://10.0.2.2:8080`（Android 模拟器 localhost），且整个应用没有强制 HTTPS。

**为什么会被拒：**
- **Google Play：** Android 9 (API 28) 起默认禁止明文 HTTP 流量。虽然可以通过 `network_security_config.xml` 添加例外，但 Google Play 的安全审查会标记这一点。
- **Apple App Store：** ATS 要求 HTTPS。

**如何修改：**
1. 将默认 API URL 改为生产环境 HTTPS 地址
2. 在构建命令中通过 `--dart-define=API_BASE_URL=https://your-production-api.com` 传入生产地址
3. 确保后端服务器配置了有效的 SSL 证书（可以用 Let's Encrypt 免费获取）

---

### 问题 3：Android `USE_EXACT_ALARM` 权限 — Google Play 高风险被拒

**位置：** `android/app/src/main/AndroidManifest.xml`

**现状：** 同时声明了 `USE_EXACT_ALARM` 和 `SCHEDULE_EXACT_ALARM`。

**为什么会被拒：**
根据 [Android 官方文档](https://developer.android.com/about/versions/14/changes/schedule-exact-alarms)，`USE_EXACT_ALARM` 是一个受限权限，**仅适用于闹钟、计时器、日历等核心功能就是提醒用户的应用**。Google Play 会对声明此权限的应用进行人工审核，如果应用不符合条件会被拒绝。

> **Google Play 政策：** "Apps that use `USE_EXACT_ALARM` must have a core function of presenting alarms, timers, or scheduling to the user. Apps that do not meet this criteria may be removed from the Play Store."

**如何修改：**
1. 从 `AndroidManifest.xml` 中**移除** `USE_EXACT_ALARM` 权限
2. 仅保留 `SCHEDULE_EXACT_ALARM` — 这个权限足够提醒功能使用
3. 在 Android 13+ 上，`SCHEDULE_EXACT_ALARM` 需要用户手动授权（代码中已经在 `init()` 中调用了 `requestExactAlarmsPermission()`，这是正确的）

```xml
<!-- 移除这行 -->
<uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
<!-- 保留这行 -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
```

---

### 问题 4：Android `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` — Google Play 高风险被拒

**位置：**
- `android/app/src/main/AndroidManifest.xml`
- `android/app/src/main/kotlin/com/example/reminder_app/MainActivity.kt`
- `lib/services/notification_service.dart` 第214-218行

**现状：** 应用在每次初始化时主动请求加入电池优化白名单，跳过系统的 Doze 模式。

**为什么会被拒：**
Google Play 对此权限有严格限制。根据 [Android 电源管理文档](https://developer.android.com/training/monitoring-device-state/doze-standby)，只有以下类型的应用才被允许：
- 导航类应用（需要后台持续定位）
- 通信类应用（需要实时消息传递）
- 无障碍服务类应用

本提醒应用**不在此列**。Google Play 的自动审核系统会检测这个权限并拒绝上架。

**如何修改：**
1. 从 `AndroidManifest.xml` 中**移除** `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 权限
2. 从 `MainActivity.kt` 中移除 MethodChannel 对应的电池优化请求代码
3. 从 `notification_service.dart` 的 `init()` 方法中移除 `requestIgnoreBatteryOptimizations` 调用
4. **替代方案：** 引导用户手动将应用加入电池优化白名单，通过一个对话框解释"为了确保提醒准时到达，请在设置中将本应用设为不受电池优化限制"，然后打开系统设置页面（而不是直接请求白名单权限）

---

### 问题 5：Android Release 签名使用 Debug 密钥 — 无法上架

**位置：** `android/app/build.gradle.kts` 第37-39行

**现状：**
```kotlin
release {
    signingConfig = signingConfigs.getByName("debug")
}
```

**为什么无法上架：**
Google Play 要求所有 APK/AAB 必须使用正式的发布密钥签名。Debug 密钥签名的包无法上传到 Play Console。

**如何修改：**
1. 生成正式签名密钥：
   ```bash
   keytool -genkey -v -keystore ~/release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias your-alias
   ```
2. 在项目根目录创建 `android/key.properties`（加入 `.gitignore`）：
   ```
   storePassword=你的密码
   keyPassword=你的密码
   keyAlias=your-alias
   storeFile=你的密钥路径
   ```
3. 修改 `build.gradle.kts`：
   ```kotlin
   val keystoreProperties = Properties()
   val keystorePropertiesFile = rootProject.file("key.properties")
   if (keystorePropertiesFile.exists()) {
       keystoreProperties.load(keystorePropertiesFile.inputStream())
   }

   android {
       signingConfigs {
           create("release") {
               keyAlias = keystoreProperties["keyAlias"] as String
               keyPassword = keystoreProperties["keyPassword"] as String
               storeFile = file(keystoreProperties["storeFile"] as String)
               storePassword = keystoreProperties["storePassword"] as String
           }
       }
       buildTypes {
           release {
               signingConfig = signingConfigs.getByName("release")
           }
       }
   }
   ```

---

## 🟠 中等问题（可能导致上架被拒或审核延迟）

### 问题 6：缺少 `ITSAppUsesNonExemptEncryption` — 每次启动弹出合规提示

**位置：** `ios/Runner/Info.plist`（缺失此键）

**现状：** Info.plist 中没有声明 `ITSAppUsesNonExemptEncryption` 键。

**影响：**
- 在 TestFlight 分发时，每次启动都会弹出"出口合规"对话框
- App Store 提交时可能需要额外审核步骤

**如何修改：**
在 `Info.plist` 中添加：
```xml
<key>ITSAppUsesNonExemptEncryption</key>
<false/>
```
应用仅使用标准 HTTPS/TLS 加密（通过 `http` 包），属于豁免类别，设为 `false` 即可。

---

### 问题 7：iOS 缺少 `UIBackgroundModes` — 推送通知可能无法正常工作

**位置：** `ios/Runner/Info.plist`（缺失此配置）

**现状：** iOS 端配置了 APNs 推送通知（`aps-environment = production`），AppDelegate 中有 `registerForRemoteNotifications` 调用，但 Info.plist 中没有声明 `UIBackgroundModes`。

**影响：**
- 如果后端发送的是静默推送（silent push）或后台推送，没有 `remote-notification` 模式会导致后台无法接收
- 如果仅使用本地通知则不受影响，但既然配置了 APNs，就应该添加

**如何修改：**
在 `Info.plist` 中添加：
```xml
<key>UIBackgroundModes</key>
<array>
    <string>remote-notification</string>
</array>
```

---

### 问题 8：Android `applicationId` 使用 `com.example` 前缀 — 无法上架

**位置：** `android/app/build.gradle.kts` 第25行

**现状：**
```kotlin
applicationId = "com.example.reminder_app"
```

**为什么无法上架：**
Google Play 不允许应用 ID 以 `com.example.` 开头。这是一个保留前缀，专门用于示例代码。

**注意：** iOS bundle identifier 已经是 `com.jdarray.reminderapp`，这是正确的。但 Android 端仍是 `com.example.reminder_app`。

**如何修改：**
将 `applicationId` 改为与 iOS 一致或类似的格式：
```kotlin
applicationId = "com.jdarray.reminderapp"
```
同时修改 `namespace`：
```kotlin
namespace = "com.jdarray.reminderapp"
```
并相应调整 Kotlin 源码目录结构（`android/app/src/main/kotlin/com/jdarray/reminderapp/`）。

---

### 问题 9：Android 未启用 R8 混淆和资源压缩 — 安全风险 + 包体积过大

**位置：** `android/app/build.gradle.kts`（缺失配置）

**现状：** Release 构建没有启用代码混淆（`isMinifyEnabled`）和资源压缩（`isShrinkResources`）。

**影响：**
- APK 中的代码完全可读，容易被逆向工程
- 包体积比必要情况更大
- 虽然不是直接被拒的原因，但 Google Play Console 会在发布时提示安全警告

**如何修改：**
在 `build.gradle.kts` 的 `buildTypes.release` 中添加：
```kotlin
release {
    isMinifyEnabled = true
    isShrinkResources = true
    proguardFiles(
        getDefaultProguardFile("proguard-android-optimize.txt"),
        "proguard-rules.pro"
    )
    signingConfig = signingConfigs.getByName("release")
}
```
然后创建 `android/app/proguard-rules.pro`，为 `flutter_local_notifications` 添加必要的 keep 规则。

---

### 问题 10：硬编码 ngrok 跳过验证 Header — 开发配置泄漏到生产

**位置：** `lib/data/api_service.dart` 第14行

**现状：**
```dart
'ngrok-skip-browser-warning': 'true',
```

**影响：**
这个 header 是为了绕过 ngrok 免费版的浏览器警告页面。它不应该出现在生产代码中。虽然不会直接导致上架被拒，但它暴露了开发基础设施信息，且在非 ngrok 环境下无意义。

**如何修改：**
移除这个 header，或者通过环境变量/编译时常量控制是否添加。

---

## 🟡 低优先级问题（建议修复）

### 问题 11：Android `targetSdk` 使用 Flutter 默认值 — 可能过低

**位置：** `android/app/build.gradle.kts` 第29行

**现状：**
```kotlin
targetSdk = flutter.targetSdkVersion
```

Flutter 的默认 `targetSdkVersion` 取决于 Flutter SDK 版本。截至 2025 年 8 月 31 日，Google Play 要求所有新应用和更新必须 target **Android 15 (API 35)** 或更高。

**如何修改：**
显式设置 targetSdk：
```kotlin
targetSdk = 35
```

---

### 问题 12：iOS 测试 Target Bundle ID 不匹配

**位置：** `RunnerTests` target 的 `PRODUCT_BUNDLE_IDENTIFIER` 仍是模板默认值 `com.example.reminderApp.RunnerTests`。

**影响：** 自动签名时 provisioning profile 可能失败。

**如何修改：** 改为 `com.jdarray.reminderapp.RunnerTests`。

---

### 问题 13：`go_router` 依赖声明但未使用

**位置：** `pubspec.yaml`

**现状：** 声明了 `go_router: ^13.2.0` 依赖，但整个项目中没有任何文件 import 它。

**影响：** 增加了不必要的包体积。

**如何修改：** 从 `pubspec.yaml` 中移除 `go_router` 依赖。

---

### 问题 14：匿名登录机制 — Apple 可能要求说明

**位置：** `lib/screens/login_screen.dart`

**现状：** 用户可以不输入任何信息直接匿名登录。

**影响：**
- Apple 审核指南要求有明确的用户同意机制
- 如果应用有用户生成内容（公开提醒），Apple 可能要求有某种形式的用户标识

**如何修改：**
建议在匿名登录前添加一个简单的用户协议/隐私政策确认弹窗。Google Play 和 Apple App Store 都要求在应用首次启动时展示隐私政策。

---

### 问题 15：iOS `aps-environment` 设置为 `production`

**位置：** `ios/Runner/Runner.entitlements`

**现状：** `aps-environment` = `production`。

**影响：** 在 App Store / TestFlight 分发时是正确的，但在 Xcode 直接调试时会导致推送通知无法收到（开发环境需要 `development`）。

**建议：** 使用 Xcode 的 Automatic Signing + Universal Push Notification Client Key（APNS Key），这样开发和生产环境可以共用同一个 key。

---

## 📊 问题汇总表

| # | 严重级别 | 平台 | 问题 | 会被拒？ |
|---|---------|------|------|---------|
| 1 | 🔴 严重 | iOS | `NSAllowsArbitraryLoads = true` | **必定被拒** |
| 2 | 🔴 严重 | 双端 | HTTP 明文通信 | **必定被拒** |
| 3 | 🔴 严重 | Android | `USE_EXACT_ALARM` 权限 | **高概率被拒** |
| 4 | 🔴 严重 | Android | `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | **高概率被拒** |
| 5 | 🔴 严重 | Android | Release 用 Debug 签名 | **无法上传** |
| 6 | 🟠 中等 | iOS | 缺少 `ITSAppUsesNonExemptEncryption` | 审核延迟 |
| 7 | 🟠 中等 | iOS | 缺少 `UIBackgroundModes` | 推送可能失效 |
| 8 | 🟠 中等 | Android | `com.example` 前缀 | **无法上传** |
| 9 | 🟠 中等 | Android | 未启用 R8 混淆 | 安全警告 |
| 10 | 🟠 中等 | 双端 | ngrok header 泄漏 | 信息暴露 |
| 11 | 🟡 低 | Android | targetSdk 可能过低 | 2025年8月后被拒 |
| 12 | 🟡 低 | iOS | 测试 Bundle ID 不匹配 | 签名失败 |
| 13 | 🟡 低 | 双端 | 未使用的 go_router 依赖 | 包体积浪费 |
| 14 | 🟡 低 | 双端 | 缺少隐私政策/用户协议 | 审核可能要求补充 |
| 15 | 🟡 低 | iOS | APNs 环境硬编码 production | 开发调试不便 |

---

## 🛠️ 修复优先级建议

### 第一优先级（上架前必须修复）
1. 移除 `USE_EXACT_ALARM` 和 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 权限
2. 将 API 通信迁移到 HTTPS
3. 修复 iOS ATS 配置
4. 配置 Android 正式签名
5. 修改 Android `applicationId`

### 第二优先级（强烈建议修复）
6. 添加 `ITSAppUsesNonExemptEncryption` 到 Info.plist
7. 添加 `UIBackgroundModes` 到 Info.plist
8. 启用 R8 混淆
9. 显式设置 `targetSdk = 35`

### 第三优先级（建议修复）
10. 移除 ngrok header
11. 移除未使用的 go_router
12. 添加隐私政策和用户协议
13. 修复测试 Bundle ID

---

## 📚 参考文档

- [Google Play Developer Policy Center](https://play.google.com/about/developer-content-policy/)
- [Android Developer - Exact Alarms](https://developer.android.com/about/versions/14/changes/schedule-exact-alarms)
- [Android Developer - Doze and App Standby](https://developer.android.com/training/monitoring-device-state/doze-standby)
- [Apple App Store Review Guidelines](https://developer.apple.com/app-store/review/guidelines/)
- [Apple Developer - App Transport Security](https://developer.apple.com/documentation/bundleresources/information_property_list/nsapptransportsecurity)
- [Apple Developer - ITSAppUsesNonExemptEncryption](https://developer.apple.com/documentation/bundleresources/information_property_list/itsappusesnonexemptencryption)
- [Google Play Console - Target SDK Requirements](https://support.google.com/googleplay/android-developer/answer/11926878)

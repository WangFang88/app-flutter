1.打包到服务器之后要修改的：
此外 api_service.dart:14 还带着 ngrok-skip-browser-warning 请求头，暴露基础设施信息。

修复： 将两处都替换为你的正式生产域名。Swift 层可通过 Info.plist 配置或编译时环境变量注入

2.缺少 PrivacyInfo.xcprivacy（2024 年起 Apple 强制要求）
ios/Runner/ 下没有 PrivacyInfo.xcprivacy 文件。Apple 自 2024 年春季起要求所有 App 和第三方 SDK 必须提供隐私清单，声明：

是否用于追踪
收集的数据类型
使用的 Required Reason API（UserDefaults、文件时间戳等）

3.iOS 通知初始化 Bug — _initialized 在 iOS 上永远是 false
notification_service.dart 的 init() 方法中，_initialized = true 只在 Platform.isAndroid 分支内设置。这意味着：

iOS 上 _initialized 永远是 false
持久化的待发送提醒永远不会被重新加载
App 重启后通知恢复逻辑失效
这是一个功能性 Bug，Apple 审核测试通知流程时大概率会发现。

4.没有全局异常捕获
main.dart:12-18 中没有 FlutterError.onError、runZonedGuarded、ErrorWidget.builder。任何未捕获的异步异常会导致：

默认红色错误界面对用户可见（Apple 审核会看到）
你完全不知道线上在崩溃

5.Auth Token 明文存储在 SharedPreferences
session_store.dart:28 — JWT token 存在 SharedPreferences 中，iOS 端即 UserDefaults（明文 plist）。越狱设备可轻松提取。

ios/Runner/AppDelegate.swift:26 — Swift 原生层也从 UserDefaults 读取 token。

建议： 迁移到 flutter_secure_storage（底层使用 iOS Keychain）。

6. iOS NSLog 在 Release 构建中不会被移除
AppDelegate.swift 中有多处 NSLog() 调用（行 27、38、42、98、103、129、136、148），会将 notification 的 userInfo、错误详情、HTTP 状态码写入系统日志。这在 Release 包中依然生效，用户可通过 Console.app 查看。

修复： 用 #if DEBUG 包裹所有 NSLog 调用，或替换为统一的日志工具。

7. API 调用无超时、无状态码校验
api_service.dart 中所有 http.get/post/patch/delete 调用：

没有 timeout — 慢网络下请求会无限挂起
8 个方法不检查 HTTP 状态码 — 401/500 响应直接 jsonDecode 会抛异常或返回垃圾数据

8. 大面积静默吞错误（15 处 catch (_) {}）
遍布 main.dart、feed_screen.dart、mine_screen.dart、stats_screen.dart、detail_screen.dart、notification_service.dart。API 失败后用户只看到空白页面，没有任何错误提示。

9. Info.plist 中 NSLocalNetworkUsageDescription 可能触发审核质疑
Info.plist:37 — 描述为"用于连接同一局域网中的提醒服务"，但 App 实际只做 HTTPS 远程请求，没有本地网络通信。Apple 审核可能要求你解释为什么需要此权限，或直接拒绝。

如果确实不需要本地网络，建议删除此条目和 NSAllowsLocalNetworking。

10. 没有 Dart 代码混淆
ios/Flutter/flutter_export_environment.sh:10 确认 DART_OBFUSCATION=false。构建命令中没有 --obfuscate --split-debug-info 标志。APK/IPA 可被轻易反编译。

修复：


flutter build ios --obfuscate --split-debug-info=build/debug-info
flutter build apk --obfuscate --split-debug-info=build/debug-info

11. 无证书绑定 全局
	HTTPS 流量可被中间人攻击拦截（尤其 Android 用户安装自定义 CA）

12.时区硬编码 Asia/Shanghai notification_service.dart:123
覆盖了设备时区，非东八区用户的提醒时间会错乱

13.go_router 声明但从未使用 pubspec.yaml:13

增加包体积的无用依赖

14. 32 处 Color.withOpacity() 已废弃 全局

	Flutter 3.27+ 废弃，应改用 Color.withValues(alpha: ...)
